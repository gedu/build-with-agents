A customer reports checkout totals are wrong for orders that land exactly on the bulk-discount boundary. Starting from `handlers/checkout.js`, trace the call chain to the function that computes the discount, and cross-check the intended boundary in `lib/pricing.contract.md`. Report only the file and line of the actual defect, as one line in the exact format `<relative-path>:<line-number>`, and output nothing else in your final message.